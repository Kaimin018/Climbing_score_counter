# Django 環境變數配置範例
# 複製此文件為 .env 並填入實際值
# 注意：.env 文件不應提交到版本控制系統

# Django 密鑰（必須更改！）
# 生成方式：python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# 調試模式（生產環境設為 False）
DEBUG=False

# 允許的主機（用逗號分隔）
# 請替換為您的實際域名和 IP 地址
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,your-ec2-ip,127.0.0.1,localhost

# CORS 設置
# 是否允許所有來源（生產環境建議設為 False）
CORS_ALLOW_ALL_ORIGINS=False

# 允許的 CORS 來源（用逗號分隔，僅在 CORS_ALLOW_ALL_ORIGINS=False 時有效）
# 請替換為您的實際域名
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# SSL 重定向（如果使用 HTTPS，配置 SSL 後設為 True）
SECURE_SSL_REDIRECT=False


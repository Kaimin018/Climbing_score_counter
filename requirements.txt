Django==4.2.7
# mysqlclient==2.2.0  # 在 Windows 上可能需要编译，使用 pymysql 作为替代
pymysql==1.1.0
djangorestframework==3.14.0
django-cors-headers==4.3.1
Pillow>=10.0.0  # 圖片處理庫，用於 ImageField（Windows 用戶建議使用預編譯版本）

# HEIC/HEIF 格式圖片處理庫（用於將 iPhone 照片轉換為 JPEG）
# 注意：在 Windows 上，pyheif 需要編譯 C 擴展，安裝較困難
# 建議 Windows 用戶使用 pillow-heif 作為替代（見下方）
# Linux/macOS 用戶：pip install pyheif（需要先安裝 libheif-dev: sudo apt-get install libheif-dev）
# Windows 用戶：pip install pillow-heif（更簡單，無需編譯）
pyheif>=0.8.0; sys_platform != "win32"  # 僅在非 Windows 系統上安裝
pillow-heif>=0.13.0; sys_platform == "win32"  # Windows 用戶使用此替代方案

beautifulsoup4>=4.12.0  # HTML 解析庫，用於測試中的 HTML 結構檢查
gunicorn>=21.2.0  # WSGI 服務器，用於生產環境
